#include <iostream>
#include <ctime>
using namespace std;
int main()
{
	setlocale(LC_ALL, "Rus");
	srand(time(NULL));
	const int n = 10;
	float mas[n];
	cout << "Массив,состоящий из " << n << " элементов " << endl;
	for(int i = 0; i < n; i++)
	{
		mas[i] = (rand() % 101 - 50) / 10.0;
	}
	for (int i = 0; i < n; i++)
	{
		cout << mas[i] << endl;
	}
	float um = 1;
	int sum = 0;
	
	for (int i = 0; i < n; i++) 
	{
		if (i % 2==0 && mas[i] && i !=0)
		{
			um *= mas[i];
		}
	}
	cout << "\n Произведение четных чисел в массиве " << endl;
	for (int i = 0; i < n; i++)
	{
		if (i % 2 == 0 && mas[i] && i != 0)
		{
				cout << mas[i] << "  ";
		}
	}
	cout <<" = "<< um  ;
	system("pause");
}