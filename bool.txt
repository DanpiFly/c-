#include <iostream>
using namespace std;
int main()
{
	setlocale(LC_ALL, "Rus");
	int h;
	int sum = 0;
	bool read = true;
	while(read == true)
	{
		cout<<"¬ведите число"<<endl;
		cin>>h;
		sum = sum + h;
		if(sum > 1000)
	    {

		 read = false;

	    }

	}
	
	system("pause");
}