#include <iostream>
#include <string>
using namespace std;
int main()
{
	setlocale(LC_ALL, "Russian");
	string plus;
	getline(cin, plus);
	char pluse = '+';
	for (int i = 0; i < plus.length(); i++)
	{
		if (plus[i] == '+' && plus[i + 1] != '+' && plus[i - 1] != '+')
		{
			plus.insert(plus.begin() + i, pluse);
			plus.insert(plus.begin() + i, pluse);
			plus.insert(plus.begin() + i, pluse);
			i = i + 3;
		}

	}
	cout << plus << endl;
	system("pause");
}