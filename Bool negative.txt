#include <iostream>
using namespace std;
int main()
{
	setlocale(LC_ALL, "Rus");
	int h;
	bool negative = false;
	cout<<"Введите 10 чисел"<<endl;
	for(int i = 0;i<=10;i++)
	{
     cout<<"Введите число "<<endl;
	 cin>>h;
	 if(h<0)
	 {
		 negative = true;
	 }
	}
	if(negative == true)
	{
		cout<<"Было"<<endl;
	}
	else
	{
		cout<<"Не было"<<endl;
	}

	system("pause");
}